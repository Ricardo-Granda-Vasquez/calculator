import { combineReducers } from 'redux'

const postsReducer = (state = [], action: { type: any; }) => {
	switch (action.type) {
		default: return state
	}
}

const rootReducer = combineReducers({

});

export default rootReducer;